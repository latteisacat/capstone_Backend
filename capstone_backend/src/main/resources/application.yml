server:
  port: 8090

spring:
  application:
    name: capstone_backend
    # H2 Database ??
  datasource:
    hikari:
      driver-class-name: org.h2.Driver
      jdbc-url: jdbc:h2:mem:test  # H2 DB ?? ??
      username: admin        # H2 DB ?? ID (??? ??)
      password: admin        # H2 DB ?? PW (??? ??)

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop       # DB ??? ?? (none, create, create-drop, update, validate)
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true      # ?? ?? ?? (??)
        show_sql: true        # ?? ?? ??
  sql:
    init:
      mode: always
  logging:
    level:
      org.hibernate.SQL: debug